import { axios } from "axios";
import React, { useEffect, useState } from "react";

const JobDetails = ({ userId }) => {
    //fetch all job details of a single user

    return <div></div>;
};

export default JobDetails;
